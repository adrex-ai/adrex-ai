# Adrex AI MCP Server
# Get your API key at https://adrex.ai/settings (after connecting your ad accounts)
ADREX_API_KEY=

# Optional — override the backend URL (defaults to Adrex hosted API)
# ADREX_API_URL=
